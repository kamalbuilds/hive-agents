{
  "name": "hive-mind-mcp-wallet",
  "version": "1.0.0",
  "description": "Embedded MCP wallet with OpenSea integration for HiveMind",
  "main": "electron.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "build": "cd renderer && npm run build",
    "mcp": "npm run build && node mcp-server.js",
    "test": "jest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.7.9",
    "electron": "^28.1.0",
    "ethers": "^6.13.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.0.0",
    "jest": "^29.0.0"
  }
}